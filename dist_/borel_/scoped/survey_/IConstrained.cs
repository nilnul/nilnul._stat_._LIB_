namespace nilnul.stat.dist_.borel_.boundaried.survey_
{
	/// <summary>
	/// before we sample, we may set the count of samples as, say, 5; This is not the <see cref="IAim"/>, which is what would be situation after, not before, sampling.
	/// The constraints can be directly appended to <see cref="survey_"/> as next namespace seg;
	/// </summary>
	interface IConstrained { }
}
