namespace nilnul.stat._dist
{
	/// <summary>
	/// for the measurable space,
	///		which is a measure from sigma Field to [0,+inf]
	///			where the sigma Field is <see cref="nilnul.set.IFamily"/>, each el of it is a sample
	/// </summary>
	public interface ISample { }
}
